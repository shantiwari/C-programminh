#include <stdio.h>
 int main()
{
    int N; 
    scanf("%d",&N);
    int count=0,n;
	  n=N;
    while(n!=0)
    {
      n=n/10;  
      count++;
    }
    printf("The number %d contains %d digits.",N,count);
}
